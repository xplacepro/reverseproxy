description "Reverse proxy"

start on runlevel [2345]
stop on runlevel [!2345]

respawn

env GOPATH=/root/go
env PATH=$PATH:$GOPATH/bin

exec $GOPATH/bin/reverseproxy --user=u --password=p --client-user=c --client-password=p > $GOPATH/bin/reverseproxy.log 2>&1